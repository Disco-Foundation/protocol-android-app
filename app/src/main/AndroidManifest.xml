<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="disco.foundation.discoprotocol">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiscoProtocol">
        <activity
            android:name=".activities.ReadQrCamera"
            android:launchMode="singleInstance"
            android:exported="false" />
        <activity
            android:name=".activities.ViewInfoActivity"
            android:launchMode="singleInstance"
            android:exported="false" />
        <activity
            android:name=".activities.PurchaseActivity"
            android:launchMode="singleInstance"
            android:exported="false" />
        <activity
            android:name=".activities.RechargeActivity"
            android:launchMode="singleInstance"
            android:exported="false" />
        <activity
            android:name=".activities.CheckInActivity"
            android:launchMode="singleInstance"
            android:exported="false"/>
        <activity
            android:name=".activities.HomeActivity"
            android:launchMode="singleInstance"
            android:exported="false" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DiscoProtocol.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>